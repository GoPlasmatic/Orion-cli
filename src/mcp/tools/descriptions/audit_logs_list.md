List audit log entries from the Orion server. Audit logs record admin actions such as creating, updating, or deleting workflows, channels, connectors, and backups.

Each entry includes the principal (who performed the action), the action type, the resource type and ID, and a timestamp. Results are ordered newest first. Supports pagination with limit and offset.
